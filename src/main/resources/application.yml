spring:
  application:
    name: interview

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/question?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&useSSL=false
    username: root
    password: 040728
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      enabled: false

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      field-strategy: NOT_EMPTY
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.example.interview.entity

app:
  max-downgrade-levels: 2