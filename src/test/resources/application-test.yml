spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/question?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&useSSL=false
    username: root
    password: 040728
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update   # 或 create-drop（测试结束会删除表）
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

# 可选：日志设为 INFO/WARN
logging:
  level:
    org.springframework: INFO
